## V2.1.0 Public Release
**Some buttons do not work on linux distributions using KDE Plasma or GNOME**
> Not many changes but has a major bug fix <

*Recommended for all users!*
Uses Java 25
To run the app run "sudo java -jar Optomizer.jar" on Mac OS and Linux and on windows run command prompt as administrator and type "java -jar Optimizer.jar"
